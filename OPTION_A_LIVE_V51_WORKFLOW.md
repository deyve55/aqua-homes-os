# Option A Live v51 Workflow

## Approved live app

- The live app is the approved v51 file: `AquaHomesOS_v51_SOLID_GOLD_BAR_GRADIENT_EDGES_APP-2.html`.
- The public GitHub Pages root link redirects to the approved v51 file.
- Treat `structured-app` and `docs` as experimental only.
- Do not use `structured-app` or `docs` as the design source.

## Required workflow

From now on:

- Preserve the v51 visual design exactly.
- Do not redesign.
- Do not rebuild the app from scratch.
- Do not convert the v51 file into a new app unless specifically instructed later.
- Do not make broad CSS rewrites.
- Do not change the approved header, logo, gold divider, premium dark styling, cards, bottom navigation, or Main Brain look unless the task specifically asks for a tiny targeted fix.
- Make only small, targeted edits to the v51 file.
- Keep each pull request small.

## Prohibited live features

Do not add or enable any of the following:

- Live payments
- Payroll processing
- Banking
- GPS
- Accounting sync
- Customer-sensitive storage
- Ledger posting
