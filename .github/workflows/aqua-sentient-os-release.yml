name: Build Aqua Sentinel OS v0.6.0 Neural Link AI Gateway Test

on:
  push:
    branches:
      - "agent/aqua-sentinel-production-apk-v0.4.1-20260725"
      - "agent/aqua-sentinel-v047-fold-voice-repair"
      - "agent/aqua-sentinel-command-center-integration-20260730"
    paths:
      - "android-app/**"
      - "encoded-assets/aqua-sentinel-launcher-v045/**"
      - "scripts/restore-aqua-sentinel-launcher-v045.sh"
      - "scripts/verify-aqua-sentinel-widget-actions-v054.sh"
      - "sentient-os-web/**"
      - "backend/**"
      - "docs/ai-backend/**"
      - "package.json"
      - "package-lock.json"
      - "tests/aqua-backend.test.mjs"
      - "tests/sentient-functional-test.mjs"
      - ".github/workflows/aqua-sentient-os-release.yml"
  pull_request:
    branches:
      - "main"
    types:
      - opened
      - synchronize
      - reopened
    paths:
      - "android-app/**"
      - "encoded-assets/aqua-sentinel-launcher-v045/**"
      - "scripts/restore-aqua-sentinel-launcher-v045.sh"
      - "scripts/verify-aqua-sentinel-widget-actions-v054.sh"
      - "sentient-os-web/**"
      - "backend/**"
      - "docs/ai-backend/**"
      - "package.json"
      - "package-lock.json"
      - "tests/aqua-backend.test.mjs"
      - "tests/sentient-functional-test.mjs"
      - ".github/workflows/aqua-sentient-os-release.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: aqua-sentinel-os-v0.6.0-neural-link-ai-gateway-test
  cancel-in-progress: false

jobs:
  build-apk:
    if: github.event_name != 'pull_request' || github.head_ref == 'agent/aqua-sentinel-command-center-integration-20260730'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Restore checksum-locked Aqua Sentinel OS launcher
        run: |
          if ! command -v convert >/dev/null; then
            sudo apt-get update
            sudo apt-get install -y imagemagick
          fi
          bash scripts/restore-aqua-sentinel-launcher-v045.sh

      - name: Run Sentinel production contract tests
        run: |
          npm ci
          npm test

      - name: Render closed-phone Neural Link and Command Center proofs
        run: |
          mkdir -p release
          CHROME_BIN="$(command -v google-chrome || command -v chromium || command -v chromium-browser)"
          test -n "$CHROME_BIN"
          "$CHROME_BIN" \
            --headless=new \
            --no-sandbox \
            --disable-gpu \
            --hide-scrollbars \
            --run-all-compositor-stages-before-draw \
            --virtual-time-budget=1800 \
            --force-device-scale-factor=1 \
            --window-size=430,932 \
            --screenshot=release/AquaSentinelOS-v0.6.0-Neural-Link-closed-phone.png \
            "file://${GITHUB_WORKSPACE}/sentient-os-web/index.html?preview=neural"
          "$CHROME_BIN" \
            --headless=new \
            --no-sandbox \
            --disable-gpu \
            --hide-scrollbars \
            --run-all-compositor-stages-before-draw \
            --virtual-time-budget=1800 \
            --force-device-scale-factor=1 \
            --window-size=430,932 \
            --screenshot=release/AquaSentinelOS-v0.6.0-Command-Center-closed-phone.png \
            "file://${GITHUB_WORKSPACE}/sentient-os-web/index.html?preview=command"
          "$CHROME_BIN" \
            --headless=new \
            --no-sandbox \
            --disable-gpu \
            --hide-scrollbars \
            --run-all-compositor-stages-before-draw \
            --virtual-time-budget=1800 \
            --dump-dom \
            "file://${GITHUB_WORKSPACE}/sentient-os-web/index.html?preview=neural" \
            > /tmp/aqua-sentinel-neural-proof.html
          "$CHROME_BIN" \
            --headless=new \
            --no-sandbox \
            --disable-gpu \
            --hide-scrollbars \
            --run-all-compositor-stages-before-draw \
            --virtual-time-budget=1800 \
            --dump-dom \
            "file://${GITHUB_WORKSPACE}/sentient-os-web/index.html?preview=command" \
            > /tmp/aqua-sentinel-command-proof.html
          test -s release/AquaSentinelOS-v0.6.0-Neural-Link-closed-phone.png
          test -s release/AquaSentinelOS-v0.6.0-Command-Center-closed-phone.png
          grep -q 'data-aqua-preview-ready="neural"' /tmp/aqua-sentinel-neural-proof.html
          grep -q 'data-panel="neural"' /tmp/aqua-sentinel-neural-proof.html
          grep -q 'AQUA SENTINEL NEURAL LINK' /tmp/aqua-sentinel-neural-proof.html
          grep -q 'data-aqua-preview-ready="command"' /tmp/aqua-sentinel-command-proof.html
          grep -q 'data-panel="command"' /tmp/aqua-sentinel-command-proof.html
          grep -q 'AQUA FILE CABINET' /tmp/aqua-sentinel-command-proof.html
          test "$(identify -format '%wx%h' release/AquaSentinelOS-v0.6.0-Neural-Link-closed-phone.png)" = "430x932"
          test "$(identify -format '%wx%h' release/AquaSentinelOS-v0.6.0-Command-Center-closed-phone.png)" = "430x932"
          ! cmp -s \
            release/AquaSentinelOS-v0.6.0-Neural-Link-closed-phone.png \
            release/AquaSentinelOS-v0.6.0-Command-Center-closed-phone.png

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android verification tools
        run: sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"

      - name: Verify approved visual source
        run: |
          python3 - <<'PY'
          import hashlib
          import struct
          from pathlib import Path

          expected = {
              "ui-hero-front-v11.png": "d78335f4ccf77cebad9dba7d985bce979aff3e31272c4c6a85bc3f211f482df7",
              "card-financial-command-front-v11.png": "3b7bf631b4a6180d1ab2bf47ce5ec4c82dad65b0dab2e4b9f4c6247da4bc5ff1",
              "card-operations-front-v11.png": "3f8f4ebf8a500d1d74ce4b728955bed1ceea25066eb2c5754f3db1f7ff67d249",
              "card-overview-front-v11.png": "d74b1b74e62e310d89d6f900f7e9d468a7bae19aa3c84a16751af68bfd31114a",
              "card-risk-monitor-front-v11.png": "1447febbbf1245e73ee177a6bb0ae91ad3e09612e6edd57de0c1a90166940d8c",
              "card-site-intelligence-front-v11.png": "3bba50e8dc2f55f534a39e5b60de928dffcf52486b1f40cd8935455455c70055",
          }
          assets = Path("sentient-os-web/assets")
          for name, digest in expected.items():
              path = assets / name
              assert path.is_file() and path.stat().st_size > 0, name
              actual = hashlib.sha256(path.read_bytes()).hexdigest()
              assert actual == digest, (name, actual)

          hero = (assets / "ui-hero-front-v11.png").read_bytes()
          assert hero[:8] == b"\x89PNG\r\n\x1a\n"
          width, height = struct.unpack(">II", hero[16:24])
          assert width > 500 and height > 500, (width, height)

          launcher = Path("android-app/icon-source/AquaSentinel-BrainCircuit-approved-v045.png")
          assert launcher.is_file() and launcher.stat().st_size > 0
          assert hashlib.sha256(launcher.read_bytes()).hexdigest() == "bc1e014886d19f10ee1b8afdca2f5bc99d6d9c9ff103c2b359cc9457da80f6c5"
          width, height = struct.unpack(">II", launcher.read_bytes()[16:24])
          assert (width, height) == (512, 512)
          for density in ("mdpi", "hdpi", "xhdpi", "xxhdpi", "xxxhdpi"):
              icon = Path(f"android-app/app/src/main/res/mipmap-{density}/aqua_sentinel_launcher.png")
              foreground = Path(f"android-app/app/src/main/res/mipmap-{density}/aqua_sentinel_launcher_foreground.png")
              assert icon.is_file() and icon.stat().st_size > 0, density
              assert foreground.is_file() and foreground.stat().st_size > 0, density
          PY

      - name: Install approved interface into APK
        run: |
          mkdir -p android-app/app/src/main/assets/public
          cp -R sentient-os-web/. android-app/app/src/main/assets/public/

      - name: Create release candidate certificate
        run: |
          KEY_PASSWORD="$(openssl rand -hex 24)"
          keytool -genkeypair \
            -keystore "$RUNNER_TEMP/aqua-sentinel-v0.6.0.keystore" \
            -storepass "$KEY_PASSWORD" \
            -keypass "$KEY_PASSWORD" \
            -alias "aqua-sentinel-v0.6.0" \
            -keyalg RSA \
            -keysize 3072 \
            -validity 3650 \
            -dname "CN=Aqua Sentinel OS v0.6.0 Neural Link AI Gateway Test, OU=Aqua Sentinel OS, O=Aqua Software Inc., L=Hudson, ST=Massachusetts, C=US"
          {
            echo "AQUA_RELEASE_KEYSTORE_PATH=$RUNNER_TEMP/aqua-sentinel-v0.6.0.keystore"
            echo "AQUA_RELEASE_KEYSTORE_PASSWORD=$KEY_PASSWORD"
            echo "AQUA_RELEASE_KEY_ALIAS=aqua-sentinel-v0.6.0"
            echo "AQUA_RELEASE_KEY_PASSWORD=$KEY_PASSWORD"
          } >> "$GITHUB_ENV"

      - name: Build release APK
        working-directory: android-app
        run: gradle --no-daemon --stacktrace :app:assembleRelease -Paqua.ecosystemPreview=true

      - name: Verify APK package, signing, and embedded interface
        run: |
          BUILD_TOOLS="$ANDROID_HOME/build-tools/35.0.0"
          APK="android-app/app/build/outputs/apk/release/app-release.apk"
          "$BUILD_TOOLS/zipalign" -c -P 16 -v 4 "$APK"
          "$BUILD_TOOLS/apksigner" verify --verbose --print-certs "$APK" | tee /tmp/aqua-sentinel-certificate.txt
          "$BUILD_TOOLS/aapt" dump badging "$APK" | tee /tmp/aqua-sentinel-badging.txt
          grep -q "package: name='com.aquahomes.sentinel'" /tmp/aqua-sentinel-badging.txt
          grep -q "versionCode='2026080101'" /tmp/aqua-sentinel-badging.txt
          grep -q "versionName='0.6.0-neural-link-ai-gateway-test'" /tmp/aqua-sentinel-badging.txt
          grep -q "sdkVersion:'26'" /tmp/aqua-sentinel-badging.txt
          grep -q "targetSdkVersion:'35'" /tmp/aqua-sentinel-badging.txt
          unzip -l "$APK" | grep -q "assets/public/index.html"
          unzip -l "$APK" | grep -q "assets/public/assets/ui-hero-front-v11.png"
          unzip -l "$APK" | grep -q "assets/public/fidelity.css"
          unzip -t "$APK"

      - name: Prove clean install and launch on Android
        uses: ReactiveCircus/android-emulator-runner@v2
        with:
          api-level: 29
          target: default
          arch: x86_64
          profile: pixel_2
          emulator-boot-timeout: 600
          disable-animations: true
          emulator-options: -no-window -gpu swiftshader -feature -Vulkan -no-snapshot -noaudio -no-boot-anim -camera-back none
          script: |
            adb uninstall com.aquahomes.sentinel || true
            adb install --no-incremental android-app/app/build/outputs/apk/release/app-release.apk
            adb shell svc power stayon true
            adb shell settings put system screen_off_timeout 2147483647
            adb shell settings put secure immersive_mode_confirmations confirmed
            adb shell input keyevent KEYCODE_WAKEUP
            adb shell wm dismiss-keyguard
            adb logcat -c
            adb shell am start -W -n com.aquahomes.sentinel/com.aquahomes.sentientos.MainActivity
            for attempt in $(seq 1 20); do sleep 2; if adb logcat -d | grep -q "AQUA_SENTINEL_UI_READY"; then break; fi; done
            adb logcat -d > /tmp/aqua-sentinel-v0.6.0-logcat.txt
            grep -q "AQUA_SENTINEL_UI_READY" /tmp/aqua-sentinel-v0.6.0-logcat.txt
            adb shell pidof com.aquahomes.sentinel | grep -q . || (grep -E "FATAL EXCEPTION|AndroidRuntime|chromium|WebViewFactory" /tmp/aqua-sentinel-v0.6.0-logcat.txt || true; exit 1)
            adb shell dumpsys package com.aquahomes.sentinel | grep -q "versionCode=2026080101"
            adb shell dumpsys package com.aquahomes.sentinel | grep -q "AquaCommandWidget"
            adb shell dumpsys package com.aquahomes.sentinel | grep -q "QuickCaptureActivity"
            adb shell dumpsys package com.aquahomes.sentinel | grep -q "EvidenceProvider"
            adb shell dumpsys activity activities > /tmp/aqua-sentinel-v0.6.0-initial-activities.txt
            grep -Eq "(mResumedActivity|topResumedActivity|ResumedActivity).*com\.aquahomes\.sentinel/com\.aquahomes\.sentientos\.MainActivity" /tmp/aqua-sentinel-v0.6.0-initial-activities.txt
            adb shell uiautomator dump /sdcard/aqua-sentinel-v0.6.0-initial-window.xml >/dev/null
            adb pull /sdcard/aqua-sentinel-v0.6.0-initial-window.xml /tmp/aqua-sentinel-v0.6.0-initial-window.xml >/dev/null
            ! grep -Eiq "Camera keeps stopping|Close app|Viewing full screen|GOT IT" /tmp/aqua-sentinel-v0.6.0-initial-window.xml
            for attempt in $(seq 1 15); do adb exec-out screencap -p > /tmp/aqua-sentinel-v0.6.0-launch.png; if test "$(identify -format '%k' /tmp/aqua-sentinel-v0.6.0-launch.png)" -gt 1; then break; fi; sleep 1; done
            test -s /tmp/aqua-sentinel-v0.6.0-launch.png
            test "$(identify -format '%k' /tmp/aqua-sentinel-v0.6.0-launch.png)" -gt 1
            bash scripts/verify-aqua-sentinel-widget-actions-v054.sh
            adb shell am force-stop com.android.camera2 || true
            adb shell input keyevent KEYCODE_BACK || true
            adb logcat -c
            adb shell am force-stop com.aquahomes.sentinel
            adb shell input keyevent KEYCODE_WAKEUP
            adb shell wm dismiss-keyguard
            adb shell am start -W -n com.aquahomes.sentinel/com.aquahomes.sentientos.MainActivity
            for attempt in $(seq 1 20); do sleep 2; if adb logcat -d | grep -q "AQUA_SENTINEL_UI_READY"; then break; fi; done
            adb logcat -d > /tmp/aqua-sentinel-v0.6.0-final-logcat.txt
            grep -q "AQUA_SENTINEL_UI_READY" /tmp/aqua-sentinel-v0.6.0-final-logcat.txt
            adb shell dumpsys activity activities > /tmp/aqua-sentinel-v0.6.0-activities.txt
            grep -Eq "(mResumedActivity|topResumedActivity|ResumedActivity).*com\.aquahomes\.sentinel/com\.aquahomes\.sentientos\.MainActivity" /tmp/aqua-sentinel-v0.6.0-activities.txt
            adb shell uiautomator dump /sdcard/aqua-sentinel-v0.6.0-window.xml >/dev/null
            adb pull /sdcard/aqua-sentinel-v0.6.0-window.xml /tmp/aqua-sentinel-v0.6.0-window.xml >/dev/null
            ! grep -Eiq "Camera keeps stopping|Close app|Viewing full screen|GOT IT" /tmp/aqua-sentinel-v0.6.0-window.xml

      - name: Prepare release files and checksums
        run: |
          mkdir -p release
          cp android-app/app/build/outputs/apk/release/app-release.apk release/AquaSentinelOS-v0.6.0-Neural-Link-AI-Gateway-Test.apk
          cp /tmp/aqua-sentinel-v0.6.0-launch.png release/AquaSentinelOS-v0.6.0-launch-proof.png
          cp /tmp/aqua-sentinel-certificate.txt release/AquaSentinelOS-v0.6.0-certificate.txt
          cp /tmp/aqua-sentinel-badging.txt release/AquaSentinelOS-v0.6.0-package.txt
          {
            echo "commit=$GITHUB_SHA"
            echo "workflow_run=$GITHUB_RUN_ID"
            echo "package=com.aquahomes.sentinel"
            echo "version=0.6.0-neural-link-ai-gateway-test"
            echo "ai_gateway=server_owned_json_rpc_2.0"
            echo "projection_sync=authenticated_idempotent_tenant_scoped"
            echo "ecosystem_presentation_mode=true"
            echo "customer_preview_snapshots=false"
            echo "target_sdk=35"
            echo "min_sdk=26"
          } > release/AquaSentinelOS-v0.6.0-provenance.txt
          sha256sum release/AquaSentinelOS-v0.6.0-Neural-Link-AI-Gateway-Test.apk > release/AquaSentinelOS-v0.6.0-Neural-Link-AI-Gateway-Test.apk.sha256

      - name: Upload verified installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: AquaSentinelOS-v0.6.0-Neural-Link-AI-Gateway-Test
          path: release/*
          if-no-files-found: error
